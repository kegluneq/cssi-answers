Add a model object (ndb.Model)
  to represent users
Include properties for
  nickname
  email
  user_id
  page_view_count
Display page_view_count on the
  html page

Algorithm
1) Query DB for user
2) If not exists
     create w/ count of 1
  else
     increment count by 1
3) Save updated DB object
